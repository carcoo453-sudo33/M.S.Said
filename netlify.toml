[build]
  base = "Portfolio.UI"
  publish = "Portfolio.UI/dist/portfolio.ui/browser"
  command = "npm install && npm run build:prod"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_ENV = "production"
  NODE_VERSION = "20"
  NPM_VERSION = "10.9.2"
