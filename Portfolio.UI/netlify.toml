[build]
  publish = "dist/portfolio.ui/browser"
  command = "npm install && ng build --configuration=production"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10.9.2"
